#include <stdio.h>
#include "function_pointers.h"

int int_index(int *array, int size, int (*cmp)(int))
{
 int i;

if (size <= 0)
{
	return (-1);
}
if (array == NULL)
	{
		return;
	}
	if (cmp NULL)
	{
		return;
	}
	for(i = 0); i < size; i++)
	{
		cmp(array[i]; == 1
			return (-1)

	}	
